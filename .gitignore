.DS_Store
.vscode/
.idea/
__pycache__/
*.py[cod]

# Virtual environments
venv/
venv_*/
.venv/

# Inference outputs
test_outputs/
out_dir/
output/
out_demo/
prediction.mmcif
prediction.cif
af3_*_out/

# Model weights and downloaded caches (huge)
checkpoints/
**/common/
~/common/

# Cached MSAs / templates
**/msa_cache/
**/template_cache/

# Notebook artefacts
*.ipynb_checkpoints/

# Note: `tutorials/` IS committed (it's generated by prepare_tutorials.py
# but we ship the snapshot so a fresh clone can start filling in blanks
# without an extra build step). Re-run prepare_tutorials.py before committing
# any solutions/ change so the two stay in sync.
# 注: ``tutorials/`` 也提交了 —— 由 prepare_tutorials.py 生成，但我们提交
# 一份快照，方便学生 clone 后直接开始。改完 solutions/ 后请重新跑
# prepare_tutorials.py 保证两边一致。

# Local upstream references — used during development only
protenix_upstream/
alphafold-decoded-main/
af3/

# Internal agent ↔ agent review communication — NOT for public release
agent_handoff/
